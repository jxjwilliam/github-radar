{
  "name": "github-radar",
  "version": "1.0.0",
  "description": "use github apis to generate and compute trends and radar.",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && nodemon ./app.js",
    "start": "cd server && node ./app.js",
    "start1": "PORT=8000 concurrently --kill-others-on-fail \"cd server && yarn start\" \"cd client/ && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "William Jiang",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
